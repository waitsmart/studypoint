js中的数据类型
=====
1原始类型
--------
原始类型是什么呢？是可以保存在变量本地的简单数据类型， 什么是变量？变量就是指，只可以保存一个值的数据空间，再起一个名字<br>
原始类型包含：number string boolean null undefined
2引用类型
--------
引用类型刚好相反，是指，不可以保存在变量本地的复杂数据类型，一般都是比较复杂的数据，比如数组，即使一个空的数组，也有push，下标等方法和属性。
引用类型包含有  array function Date Object
### 他们的区别
原始类型可以保存在本地，引用类型不能。举个栗子：比如函数，我们都知道，函数名实际上就是一个变量名，而函数就是一个对象，这样的话，它的保存是这样子的，
保存函数名作为变量名，然后他的值，不是函数这个对象，而是一个地址，这个地址我们不知道，但是电脑知道（大概吧），他是通过这个地址来引用这个函数，所以
这种类型都叫做引用类型。


* * *
var name="lilei"
`${name}`
这个是模板字符串，里面有$  是和模板字符串配套的。这个知识点记录是因为，我忘记了这个叫什么名字了。模板字符串。


